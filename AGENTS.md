## graphify (workspace knowledge graph)

This directory is part of the `/home/lu47/Desktop/Lucholabs` workspace, which has a
graphify knowledge graph at the workspace root (`graphify-out/`). Mandatory rules:
**read `/home/lu47/Desktop/Lucholabs/GRAPHIFY.md`** before architecture analysis and
after any code change (it requires `graphify update .` run from the workspace root,
with verification). Root workspace instructions live in
`/home/lu47/Desktop/Lucholabs/CLAUDE.md` and `AGENTS.md`.
